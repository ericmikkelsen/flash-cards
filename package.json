{
  "name": "flash-cards",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node dev.js",
    "start": "npm run dev",
    "build": "mkdir -p dist && node build.js"
  }
}
